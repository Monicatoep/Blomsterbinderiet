@page
@model Blomsterbinderiet.Pages.UserPages.MyOrdersModel
@{
}
<h1>Dine ordrer</h1>
<table class="table">
    <thead>
        <tr>
            <th>Bestillings dato</th>
            <th>Status</th>
            <th>ID</th>
            <th>Mængde</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Orders)
        {
            <tr>
                <td>@item.hello.OrderDate</td>
                <td>@item.hello.OrderStatus</td>
                <td>@item.hello.Id</td>
                <td>@item.Amount</td>
            </tr>
        }
    </tbody>
</table>